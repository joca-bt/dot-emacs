;; -----------------------------------------------------------------------------
;; json ------------------------------------------------------------------------

;; indentation
(setq json-reformat:indent-width 4)

;; -----------------------------------------------------------------------------
;; protobuf --------------------------------------------------------------------

;; indentation
(c-add-style "protobuf" '((c-basic-offset . 4)))
(add-to-list 'c-default-style '(protobuf-mode . "protobuf"))

;; -----------------------------------------------------------------------------
;; xml -------------------------------------------------------------------------

;; indentation
(setq nxml-attribute-indent 4
      nxml-child-indent 4
      nxml-outline-child-indent 4)

;; -----------------------------------------------------------------------------
;; yaml ------------------------------------------------------------------------

;; indentation
(setq yaml-indent-offset 2)

;; -----------------------------------------------------------------------------
;; -----------------------------------------------------------------------------

(provide 'set-data)
