;; -----------------------------------------------------------------------------
;; batch -----------------------------------------------------------------------

;; major mode
(require 'cmd-mode)
(setq auto-mode-alist (append auto-mode-alist '(("\\.\\(bat\\|cmd\\)\\'" . cmd-mode))))


;; -----------------------------------------------------------------------------
;; -----------------------------------------------------------------------------

(provide 'config-shell-script)
