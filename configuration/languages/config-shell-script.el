;; -----------------------------------------------------------------------------
;; batch -----------------------------------------------------------------------

;; major mode
(require 'cmd-mode)
(setq auto-mode-alist (append '(("\\.\\(bat\\|cmd\\)\\'" . cmd-mode))
                              auto-mode-alist))


;; -----------------------------------------------------------------------------
;; -----------------------------------------------------------------------------

(provide 'config-shell-script)
