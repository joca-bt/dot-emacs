;; -----------------------------------------------------------------------------
;; batch -----------------------------------------------------------------------

;; batch mode
(require 'cmd-mode)
(add-to-list 'auto-mode-alist '("\\.\\(bat\\|cmd\\)\\'" . cmd-mode))


;; -----------------------------------------------------------------------------
;; -----------------------------------------------------------------------------

(provide 'config-shell-script)
