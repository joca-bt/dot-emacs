;; -----------------------------------------------------------------------------
;; batch script ----------------------------------------------------------------

;; batch script mode
(require 'cmd-mode)
(add-to-list 'auto-mode-alist '("\\.\\(bat\\|cmd\\)\\'" . cmd-mode))


;; -----------------------------------------------------------------------------
;; shell script ----------------------------------------------------------------

;; shell script mode
(require 'sh-script)


;; -----------------------------------------------------------------------------
;; -----------------------------------------------------------------------------

(provide 'config-shell-script)
